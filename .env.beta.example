# Beta Environment Variables Template
# Copy this to .env.beta and fill in actual values
# NEVER commit .env.beta to git (it's in .gitignore)

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=production
PORT=5000

# ===========================================
# DATABASE (PostgreSQL)
# ===========================================
# Provided by Render automatically
DATABASE_URL=postgresql://user:password@host:5432/authdb_beta

# ===========================================
# REDIS CACHE
# ===========================================
# Provided by Render automatically
REDIS_URL=redis://host:6379

# ===========================================
# JWT CONFIGURATION
# ===========================================
# Generate with: openssl rand -hex 32
JWT_SECRET=your-beta-jwt-secret-min-32-characters
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=7d

# ===========================================
# MFA (Multi-Factor Authentication)
# ===========================================
# Generate with: openssl rand -hex 32
# Can reuse production key or generate new for beta
MFA_ENCRYPTION_KEY=your-beta-mfa-encryption-key-64-hex-chars

# ===========================================
# FRONTEND URL
# ===========================================
# Update after Render deployment
FRONTEND_URL=https://auth-frontend-beta.onrender.com

# ===========================================
# EMAIL (SendGrid) - Optional for Beta
# ===========================================
# Use test API key or skip if not testing email features
SENDGRID_API_KEY=SG.your-sendgrid-api-key
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# ===========================================
# OAUTH - GOOGLE (Optional for Beta)
# ===========================================
# Use separate OAuth app for beta or skip if not testing OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://auth-backend-beta.onrender.com/api/auth/google/callback

# ===========================================
# OAUTH - GITHUB (Optional for Beta)
# ===========================================
# Use separate OAuth app for beta or skip if not testing OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_CALLBACK_URL=https://auth-backend-beta.onrender.com/api/auth/github/callback

# ===========================================
# NOTES
# ===========================================
# 1. All SENSITIVE values (secrets, API keys) must be added
#    manually via Render dashboard, not in render.yaml
#
# 2. DATABASE_URL and REDIS_URL are auto-injected by Render
#    from the database/redis services
#
# 3. Update callback URLs after getting actual Render URLs
#
# 4. For testing without email: You can skip SENDGRID vars
#    and use dummy email provider or disable email features
#
# 5. For testing without OAuth: You can skip OAuth vars
#    and test only email/password auth + MFA
